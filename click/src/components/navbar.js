import React from "react";


const Navbar = () => (
	<div className="navbar">
		<span>Memory Game!</span>
		<span className="message">Let's Play!</span>
		<span>Score: | Top Score: </span>

	</div>
);

export default Navbar;

