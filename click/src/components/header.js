import React from "react";


const Header = () => (
	<header className="header">
		<h1 className="text-center">Memory Game</h1>
		<b><p className="text-center text-danger">Click a Corgi puppy to earn points, but don't click on the same Corgi twice!</p></b>

	</header>
);

export default Header;

